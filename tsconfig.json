{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Canonical TypeScript config lives in config/tsconfig.json. This file extends it with root-relative paths.",
  "extends": "./config/tsconfig.json",
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
